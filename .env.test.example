# Roomora test credentials for the tester sub-agent — copy to .env.test. NEVER commit .env.test.
ROOMORA_TEST_EMAIL=tester@roomora.test
ROOMORA_BASE_URL=http://localhost:3000
# Dev-login token (must match server; gated by TEST_USER_EMAILS). Phase 2+.
ROOMORA_DEV_LOGIN_TOKEN=
