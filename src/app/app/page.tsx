import { LandingScreen } from "@/components/screens/LandingScreen";

/** The app entry (upload + funnel). The marketing site lives at "/"; the app
 *  itself starts here at "/app", inside the phone-frame chrome. */
export default function AppHomePage() {
  return <LandingScreen />;
}
